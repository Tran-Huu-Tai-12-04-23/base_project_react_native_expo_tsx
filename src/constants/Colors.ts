const tintColorLight = '#2f95dc';
const tintColorDark = '#fff';
const primaryColor = '#F05A22';
const btnPrimary = '#F05A22';
const secondaryColor = '#F9C5B9';
const blackColor = '#000';
const whiteColor = '#fff';
const borderColor = '#767FAD';

export { primaryColor, btnPrimary, secondaryColor, blackColor, whiteColor, borderColor };

export default {
   light: {
      text: '#000',
      background: '#fff',
      tint: tintColorLight,
      tabIconDefault: '#ccc',
      tabIconSelected: tintColorLight,
   },
   dark: {
      text: '#fff',
      background: '#000',
      tint: tintColorDark,
      tabIconDefault: '#ccc',
      tabIconSelected: tintColorDark,
   },
};
