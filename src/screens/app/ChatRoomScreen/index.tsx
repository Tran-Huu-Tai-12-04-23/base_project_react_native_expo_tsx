import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const ChatRoomScreen = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="ChatRoomScreen" align="center" />
    </FView>
  );
};

export default ChatRoomScreen;
