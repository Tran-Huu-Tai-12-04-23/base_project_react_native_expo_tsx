import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const StationFareScreen = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="StationFareScreen" align="center" />
    </FView>
  );
};

export default StationFareScreen;
