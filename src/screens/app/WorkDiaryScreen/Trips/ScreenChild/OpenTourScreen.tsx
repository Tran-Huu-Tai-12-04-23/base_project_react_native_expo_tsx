import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const OpenTourScreen = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="OpenTourScreen" align="center" />
    </FView>
  );
};

export default OpenTourScreen;
