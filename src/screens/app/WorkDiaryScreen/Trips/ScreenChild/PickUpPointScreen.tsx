import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const PickUpPointScreen = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="PickUpPointScreen" align="center" />
    </FView>
  );
};

export default PickUpPointScreen;
