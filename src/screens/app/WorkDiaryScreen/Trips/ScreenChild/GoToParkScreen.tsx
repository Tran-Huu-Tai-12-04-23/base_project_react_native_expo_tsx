import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const GoToParkScreen = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="GoToParkScreen" align="center" />
    </FView>
  );
};

export default GoToParkScreen;
