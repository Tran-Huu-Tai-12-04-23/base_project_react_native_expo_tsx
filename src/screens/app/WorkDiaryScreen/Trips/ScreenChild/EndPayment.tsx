import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const EndPayment = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="EndPayment" align="center" />
    </FView>
  );
};

export default EndPayment;
