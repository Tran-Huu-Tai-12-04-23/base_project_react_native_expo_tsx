import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const CreateStationFareScreen = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="CreateStationFareScreen" align="center" />
    </FView>
  );
};

export default CreateStationFareScreen;
