import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const PauseTourScreen = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="PauseTourScreen" align="center" />
    </FView>
  );
};

export default PauseTourScreen;
