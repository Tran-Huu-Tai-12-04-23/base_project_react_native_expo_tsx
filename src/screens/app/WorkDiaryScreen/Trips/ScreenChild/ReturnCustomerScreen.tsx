import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const ReturnCustomerScreen = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="ReturnCustomerScreen" align="center" />
    </FView>
  );
};

export default ReturnCustomerScreen;
