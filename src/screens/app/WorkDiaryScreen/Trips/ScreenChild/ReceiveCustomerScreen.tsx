import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const ReceiveCustomerScreen = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="ReceiveCustomerScreen" align="center" />
    </FView>
  );
};

export default ReceiveCustomerScreen;
