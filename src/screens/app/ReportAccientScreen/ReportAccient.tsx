import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const ModelChooseCar = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="ModelChooseCar" align="center" />
    </FView>
  );
};

export default ModelChooseCar;
