import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const ChatSocketScreen = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="ChatSocketScreen" align="center" />
    </FView>
  );
};

export default ChatSocketScreen;
