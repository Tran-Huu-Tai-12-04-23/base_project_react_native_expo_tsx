import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const DetailNotifyAlert = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="DetailNotifyAlert" align="center" />
    </FView>
  );
};

export default DetailNotifyAlert;
