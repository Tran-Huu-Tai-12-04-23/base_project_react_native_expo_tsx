import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const DetailRequest = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="DetailRequest" align="center" />
    </FView>
  );
};

export default DetailRequest;
