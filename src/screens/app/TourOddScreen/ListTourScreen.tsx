import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const ListTourScreen = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="ListTourScreen" align="center" />
    </FView>
  );
};
 
export default ListTourScreen;
