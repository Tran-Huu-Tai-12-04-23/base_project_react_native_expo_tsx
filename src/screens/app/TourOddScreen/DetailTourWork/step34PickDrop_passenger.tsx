import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const step34PickDrop_passenger = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="step34PickDrop_passenger" align="center" />
    </FView>
  );
};

export default step34PickDrop_passenger;
