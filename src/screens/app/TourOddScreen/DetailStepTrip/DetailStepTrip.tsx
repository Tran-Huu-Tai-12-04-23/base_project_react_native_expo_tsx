import { FHeader, FView } from "@components/index";
import React from "react";

type IProps = {};
const DetailStepTrip = ({}: IProps) => {
  return (
    <FView>
      <FHeader type="menu" title="DetailStepTrip" align="center" />
    </FView>
  );
};

export default DetailStepTrip;
